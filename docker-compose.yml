services:
  argeninta:
    build: .
    container_name: argeninta
    restart: unless-stopped
    ports:
      - "127.0.0.1:3002:3001"
    env_file:
      - .env.local
    networks:
      - caddy_default

networks:
  caddy_default:
    external: true
